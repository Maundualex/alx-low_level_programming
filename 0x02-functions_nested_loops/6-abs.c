#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @z: THe int to be operated upon
 *
 * Return: returns an unsigneed int value to calling function
 */

int _abs(int z)
{
	if (z > 0)
	{
		return (z);
	}

	else
	{
		z *= -1;
		return (n);
	}
}
